title: Welcome to Wakkalabs
email: acarter@wakkalabs.com
description: "The worlds oldest long fortgotten domain parking garage rooftop."
domain: wakkalabs.github.io # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
# the base hostname and protocol for your site, e.g. http://example.com
baseurl: /

url: "https://wakkalabs.github.io" # the base hostname & protocol for your site, e.g. http://example.com
github_username: acarter-ims
# place folder name if the site is served in a subfolder
show_downloads: true
google_analytics:
theme: jekyll-theme-hacker
plugins:
- jekyll-feed
- jekyll-titles-from-headings
titles_from_headings:
  enabled: true
  strip_title: true
  collections: true
